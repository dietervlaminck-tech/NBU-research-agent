flask>=3.0
anthropic>=0.50
msal>=1.28
python-dotenv>=1.0
markdown>=3.6
pandas>=2.2
numpy>=1.26
scipy>=1.13
statsmodels>=0.14
pyreadstat>=1.2
pyreadr>=0.5
python-docx>=1.1
openpyxl>=3.1
gunicorn>=22.0
certifi>=2024.0

# Optional — only needed where the Refinitiv connector runs (desktop or platform).
# Not installed by default to keep the Azure image lean. See docs/REFINITIV_DESIGN.md.
# lseg-data>=2.0
